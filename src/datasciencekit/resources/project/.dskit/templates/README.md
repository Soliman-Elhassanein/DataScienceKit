# DataScienceKit templates

Project-local template overrides belong here. Core study templates are copied
from the installed `datasciencekit` package when `dskit new` runs. Override one
by adding a file with the same name as one of the ten IBM methodology artifacts
listed by `dskit status`.
